# Endpoint do Gotenberg para "Baixar PDF".
# Vazio (padrão) → usa o proxy /gotenberg do Vite (docker compose local).
# Em produção, aponte para o seu BFF (que encapsula o Gotenberg com auth/rate-limit).
VITE_GOTENBERG_URL=
