# pagedjs traz core-js/es5-ext com build scripts (postinstall de funding) que não
# precisamos rodar. A verificação de deps antes de cada script estava abortando
# `pnpm test/build` por causa disso — desligada aqui.
verify-deps-before-run=false
